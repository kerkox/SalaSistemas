<nav class="red">
  <div class="container">
    <div class="nav-wrapper">
      <%= link_to 'login', root_path, :class => 'btn red white-text' %>
      <%= link_to 'periods', periods_path, :class => 'btn white red-text' %>
      <%= link_to 'reservations', reservations_path, :class => 'btn red white-text' %>
      <%= link_to 'rooms', rooms_path, :class => 'btn red white-text' %>
      <%= link_to 'users', users_path, :class => 'btn red white-text' %>
    </div>
  </div>
</nav>

<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Listing Periods</h1>

  <table>
    <thead>
      <tr>
        <th>Fecha inicio</th>
        <th>Fecha finalizacion</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @periods.each do |period| %>
        <tr>
          <td><%= period.fecha_inicio %></td>
          <td><%= period.fecha_finalizacion %></td>
          <td><%= link_to 'Show', period, :class => "btn white red-text" %></td>
          <td><%= link_to 'Edit', edit_period_path(period), :class => "btn white red-text" %></td>
          <td><%= link_to 'Destroy', period, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn red white-text" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Period', new_period_path, :class  => "btn white red-text" %>
</div>